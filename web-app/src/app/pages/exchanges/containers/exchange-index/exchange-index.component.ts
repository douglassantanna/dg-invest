import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-exchange-index',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './exchange-index.component.html',
})
export class ExchangeIndexComponent {}
